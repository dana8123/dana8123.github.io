{
  "name": "example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "copy": "cp -r -f ./dist/* ../ && cp -f ./dist/.nojekyll ../"
  },
  "dependencies": {
    "core-js": "^3.21.1",
    "katex": "^0.15.2",
    "nuxt": "^2.15.8",
    "prismjs": "^1.27.0",
    "vue-katex": "^0.5.0"
  },
  "devDependencies": {
    "vue-notion": "^1.4.3"
  }
}
